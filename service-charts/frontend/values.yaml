replicaCount: 1

image:
  repository: reyewkha/microservice-frontend
  tag: latest
  pullPolicy: Always

service:
  type: NodePort
  port: 30080

resources:
  limits:
    memory: "512Mi"
  requests:
    cpu: "200m"
    memory: "256Mi"