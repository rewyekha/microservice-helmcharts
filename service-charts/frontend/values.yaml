replicaCount: 1

image:
  repository: my/craftista-frontend
  tag: latest
  pullPolicy: Always

service:
  type: clusterIP
  port: 80

resources:
  limits:
    memory: "512Mi"
  requests:
    cpu: "100m"
    memory: "256Mi"